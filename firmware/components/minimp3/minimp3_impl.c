// minimp3.h is a stb-style single-header library: the declarations are
// always visible, but the implementation only gets compiled in wherever
// MINIMP3_IMPLEMENTATION is defined before including it. This is that one
// place, so the rest of the project can just #include "minimp3.h" normally.
#define MINIMP3_IMPLEMENTATION
#include "minimp3.h"
